import { NextResponse } from "next/server";import { createClient } from "@/lib/supabase/server";import { createAdminClient } from "@/lib/supabase/admin";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const {data,error}=await sb.from("builds").select("*").eq("id",id).single();if(error||!data)return NextResponse.json({error:"Build tidak ditemukan"},{status:404});let downloadUrl:string|null=null,aabDownloadUrl:string|null=null;const admin=createAdminClient();if(data.apk_path){const {data:signed}=await admin.storage.from("apks").createSignedUrl(data.apk_path,600);downloadUrl=signed?.signedUrl||null}if(data.aab_path){const {data:signed}=await admin.storage.from("apks").createSignedUrl(data.aab_path,600);aabDownloadUrl=signed?.signedUrl||null}return NextResponse.json({build:data,downloadUrl,aabDownloadUrl});}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await createClient();const {data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const {data}=await sb.from("builds").select("source_path,app_icon_path,apk_path,aab_path,status").eq("id",id).single();if(!data)return NextResponse.json({error:"Build tidak ditemukan"},{status:404});if(data.status==="building")return NextResponse.json({error:"Build aktif tidak dapat dihapus"},{status:409});const admin=createAdminClient();const sources=[data.source_path,data.app_icon_path].filter(Boolean) as string[],outputs=[data.apk_path,data.aab_path].filter(Boolean) as string[];if(sources.length)await admin.storage.from("sources").remove(sources);if(outputs.length)await admin.storage.from("apks").remove(outputs);const {error}=await sb.from("builds").delete().eq("id",id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
