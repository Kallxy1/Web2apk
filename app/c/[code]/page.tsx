import type {Metadata} from "next";import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {LazyBuildForm} from "@/components/lazy-build-form";import {BuildStatusView} from "@/components/build-status-view";import type {Build} from "@/lib/types";
export const metadata:Metadata={title:"Create App",robots:{index:false,follow:false}};
export default async function CreatePage({params}:{params:Promise<{code:string}>}){const {code}=await params;if(!/^[a-f0-9]{14}$/.test(code))notFound();const sb=await createClient();const {data}=await sb.from("builds").select("*").eq("public_code",code).maybeSingle();if(data)return <BuildStatusView initial={data as Build}/>;return <div className="mx-auto w-full max-w-5xl"><div className="enter"><p className="eyebrow">Secure build session · {code}</p><h1 className="mt-3 text-3xl font-black tracking-[-.04em] sm:text-4xl">Buat aplikasi baru.</h1><p className="mt-2 max-w-2xl text-xs leading-6 text-zinc-600">Konfigurasi tersimpan pada URL sesi privat ini setelah build dibuat.</p></div><LazyBuildForm code={code}/></div>}
