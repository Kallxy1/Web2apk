import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {BuildStatusView} from "@/components/build-status-view";import type {Build} from "@/lib/types";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,sb=await createClient();const [{data},{data:profile}]=await Promise.all([sb.from("builds").select("*").eq("id",id).single(),sb.from("profiles").select("plan,role").maybeSingle()]);if(!data)notFound();const canUpdate=profile?.role==="admin"||profile?.plan==="pro"||profile?.plan==="business";return <BuildStatusView initial={data as Build} canUpdate={canUpdate}/>}
