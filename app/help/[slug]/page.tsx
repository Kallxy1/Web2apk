import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {ArrowLeft,Check} from "lucide-react";import {SiteFooter,SiteHeader} from "@/components/site-chrome";import {helpArticles} from "@/lib/help-content";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,a=helpArticles[slug];return a?{title:a.title,description:a.description}:{title:"Not Found"}}
export function generateStaticParams(){return Object.keys(helpArticles).map(slug=>({slug}))}
export default async function HelpArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,a=helpArticles[slug];if(!a)notFound();return <main className="mesh min-h-screen"><SiteHeader/><article className="mx-auto max-w-4xl px-5 py-16 sm:px-7"><Link href="/help" className="inline-flex items-center gap-2 text-[9px] font-black uppercase tracking-widest text-zinc-700 hover:text-white"><ArrowLeft size={12}/>Help Center</Link><p className="eyebrow mt-10">{a.category}</p><h1 className="mt-4 text-5xl font-black tracking-[-.05em]">{a.title}</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-zinc-500">{a.description}</p><div className="mt-12 divide-y divide-white/[.08] border-y border-white/[.08]">{a.sections.map((section,i)=><section className="py-8" key={section.title}><div className="grid gap-4 sm:grid-cols-[42px_1fr]"><span className="text-[10px] font-black text-zinc-800">{String(i+1).padStart(2,"0")}</span><div><h2 className="text-base font-black">{section.title}</h2><p className="mt-3 text-sm leading-7 text-zinc-500">{section.body}</p>{section.items&&<ul className="mt-4 grid gap-2">{section.items.map(x=><li key={x} className="flex items-center gap-2 text-xs text-zinc-600"><Check size={12}/>{x}</li>)}</ul>}</div></div></section>)}</div></article><SiteFooter/></main>}
