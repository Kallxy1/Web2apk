import type {Metadata} from "next";import Link from "next/link";import {BookOpen,ChevronRight,FileText,LockKeyhole,MessageCircle,ShieldCheck} from "lucide-react";import {SiteFooter,SiteHeader} from "@/components/site-chrome";import {helpArticles} from "@/lib/help-content";
export const metadata:Metadata={title:"Help Center",description:"Dokumentasi, panduan, kebijakan, status, dan bantuan Web2APK."};const helpBase=process.env.NEXT_PUBLIC_HELP_URL||"/help";
export default function Help(){const categories=[...new Set(Object.values(helpArticles).map(x=>x.category))];return <main className="mesh min-h-screen"><SiteHeader/><section className="mx-auto max-w-6xl px-5 py-20 sm:px-7"><p className="eyebrow">XyStudio&apos;s support</p><h1 className="mt-4 text-5xl font-black tracking-[-.05em]">Help & Information.</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-zinc-500">Panduan builder, release Android, keamanan, kebijakan, status, dan dukungan dalam satu pusat informasi.</p><div className="mt-10 grid gap-px border border-white/[.08] bg-white/[.08] sm:grid-cols-2 lg:grid-cols-5">{[["/faq",MessageCircle,"FAQ"],["/terms",FileText,"Terms"],["/privacy",LockKeyhole,"Privacy"],["/cookies",BookOpen,"Cookies"],["/security",ShieldCheck,"Security"]].map(([href,Icon,label])=><Link key={href as string} href={href as string} className="bg-[#080808] p-4 text-xs font-black hover:bg-[#0d0d0d]"><Icon size={15} className="mb-5 text-zinc-600"/>{label as string}</Link>)}</div><div className="mt-14 space-y-14">{categories.map(category=><section key={category}><p className="eyebrow">{category}</p><div className="mt-4 grid gap-px border border-white/[.08] bg-white/[.08] sm:grid-cols-2 lg:grid-cols-3">{Object.entries(helpArticles).filter(([,a])=>a.category===category).map(([slug,a])=><Link key={slug} href={`${helpBase}/${slug}`} className="group bg-[#080808] p-5 hover:bg-[#0d0d0d]"><div className="flex justify-between"><BookOpen size={15} className="text-zinc-700"/><ChevronRight size={14} className="text-zinc-800 group-hover:text-white"/></div><h2 className="mt-6 text-xs font-black">{a.title}</h2><p className="mt-2 text-[10px] leading-5 text-zinc-600">{a.description}</p></Link>)}</div></section>)}</div></section><SiteFooter/></main>}
