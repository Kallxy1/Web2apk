import type {Metadata} from "next";import Link from "next/link";import {Check,MoveRight} from "lucide-react";import {SiteFooter,SiteHeader} from "@/components/site-chrome";import {PLANS} from "@/lib/plans";
export const metadata:Metadata={title:"Pricing",description:"Paket Web2APK Free, Pro, dan Business untuk build aplikasi Android."};
export default function Pricing(){return <main className="mesh min-h-screen"><SiteHeader/><section className="mx-auto max-w-7xl px-5 py-20 sm:px-7"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">Simple pricing</p><h1 className="mt-4 text-5xl font-black tracking-[-.055em]">Mulai gratis. Naik saat siap.</h1><p className="mt-5 text-sm leading-7 text-zinc-500">Pilih kapasitas build yang sesuai. Tidak ada biaya tersembunyi.</p></div><div className="mt-14 grid gap-px overflow-hidden border border-white/[.09] bg-white/[.09] lg:grid-cols-3">{(Object.entries(PLANS) as [keyof typeof PLANS,(typeof PLANS)[keyof typeof PLANS]][]).map(([id,p],i)=><article key={id} className={`relative bg-[#080808] p-7 sm:p-8 ${i===1?"lg:bg-[#0d0d0d]":""}`}>{i===1&&<span className="absolute right-5 top-5 bg-white px-2 py-1 text-[8px] font-black uppercase tracking-widest text-black">Popular</span>}<p className="eyebrow">{p.name}</p><p className="mt-6 text-4xl font-black tracking-tight">{p.price}<small className="text-xs font-medium text-zinc-600">{id!=="free"?"/bulan":""}</small></p><p className="mt-3 text-[10px] text-zinc-600">Hingga {p.dailyBuilds} build per hari</p><ul className="mt-8 space-y-4">{p.features.map(x=><li key={x} className="flex items-center gap-3 text-xs text-zinc-400"><Check size={13}/>{x}</li>)}</ul><Link href={id==="free"?"/register":"/register"} className={i===1?"btn-primary mt-9 w-full":"btn-secondary mt-9 w-full"}>{id==="free"?"Mulai gratis":"Gabung waitlist"}<MoveRight size={14}/></Link></article>)}</div><p className="mt-7 text-center text-[10px] text-zinc-700">Pembayaran Pro dan Business akan diaktifkan setelah payment gateway production terhubung.</p></section><SiteFooter/></main>}
