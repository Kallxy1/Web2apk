"use client";
import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {ArrowRight,Eye,EyeOff,Loader2} from "lucide-react";import {TurnstileBox} from "./turnstile-box";
function readable(message:string){if(/invalid path/i.test(message))return "Konfigurasi layanan akun tidak valid.";if(/already registered/i.test(message))return "Email sudah terdaftar. Silakan masuk.";if(/invalid login/i.test(message))return "Email atau password tidak sesuai.";return message}
export function AuthForm({mode}:{mode:"login"|"register"}){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[loading,setLoading]=useState(false),[show,setShow]=useState(false),[turnstileToken,setTurnstileToken]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage("");try{const r=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,turnstileToken})});const json=await r.json();if(!r.ok)throw new Error(json.error||"Autentikasi gagal");if(mode==="login"){router.push("/dashboard");router.refresh()}else setMessage(json.message)}catch(error){setMessage(readable(error instanceof Error?error.message:"Autentikasi gagal"))}setLoading(false)}return <form onSubmit={submit} className="glass w-full max-w-md rounded-lg p-7 sm:p-9"><p className="eyebrow">XyStudio&apos;s Account</p><h1 className="mt-3 text-3xl font-black tracking-[-.04em]">{mode==="login"?"Masuk ke workspace.":"Buat workspace."}</h1><p className="mt-2 text-xs leading-6 text-zinc-600">{mode==="login"?"Lanjutkan mengelola build aplikasi Anda.":"Satu akun untuk seluruh project Android."}</p><label className="mt-8 block"><span className="label">Alamat email</span><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email" placeholder="nama@email.com"/></label><label className="mt-5 block"><span className="label">Password</span><span className="relative block"><input className="input pr-12" type={show?"text":"password"} minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required autoComplete={mode==="login"?"current-password":"new-password"} placeholder="Minimal 8 karakter"/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?"Sembunyikan password":"Tampilkan password"} className="absolute right-1 top-1/2 grid h-9 w-10 -translate-y-1/2 place-items-center text-zinc-600 hover:text-white">{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></span></label>{mode==="login"&&<div className="mt-3 text-right"><Link href="/forgot-password" className="text-[10px] font-bold text-zinc-500 hover:text-white">Lupa password?</Link></div>}<TurnstileBox onToken={setTurnstileToken}/>{message&&<p className="mt-5 border border-white/10 bg-white/[.04] p-3 text-xs leading-5 text-zinc-400">{message}</p>}<button className="btn-primary mt-6 w-full" disabled={loading}>{loading?<Loader2 className="animate-spin" size={15}/>:null}{mode==="login"?"Masuk":"Buat akun"}<ArrowRight size={14}/></button><p className="mt-6 text-center text-[10px] text-zinc-600">{mode==="login"?"Belum punya akun? ":"Sudah punya akun? "}<Link className="font-black text-white" href={mode==="login"?"/register":"/login"}>{mode==="login"?"Daftar sekarang":"Masuk"}</Link></p></form>}
