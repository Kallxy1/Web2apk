"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import { Boxes, LayoutDashboard, LogOut, Plus } from "lucide-react"; import { Logo } from "./logo"; import { createClient } from "@/lib/supabase/client";
export function DashboardShell({children,email}:{children:React.ReactNode,email:string}){const path=usePathname();const router=useRouter();async function logout(){await createClient().auth.signOut();router.push("/");router.refresh();}const nav=[["/dashboard",LayoutDashboard,"Dashboard"],["/dashboard/new",Plus,"Build baru"]] as const;return <div className="min-h-screen lg:grid lg:grid-cols-[240px_1fr]"><aside className="border-b border-white/5 bg-panel/50 p-5 lg:sticky lg:top-0 lg:h-screen lg:border-b-0 lg:border-r"><Logo/><nav className="mt-8 flex gap-2 lg:block">{nav.map(([href,Icon,label])=><Link key={href} href={href} className={`mb-2 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${path===href?"bg-cyan/10 font-bold text-cyan":"text-slate-500 hover:bg-white/5 hover:text-white"}`}><Icon size={17}/>{label}</Link>)}</nav><div className="mt-5 border-t border-white/5 pt-5 lg:absolute lg:bottom-5 lg:left-5 lg:right-5"><p className="truncate px-3 text-xs text-slate-500">{email}</p><button onClick={logout} className="mt-2 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm text-slate-500 hover:bg-red-500/10 hover:text-red-300"><LogOut size={16}/>Keluar</button></div></aside><main className="grid-bg min-w-0 p-5 sm:p-8 lg:p-10">{children}</main></div>}
