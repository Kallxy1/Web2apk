"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {LayoutDashboard,LogOut,Menu,Plus,X} from "lucide-react";import {useState} from "react";import {Logo} from "./logo";import {createClient} from "@/lib/supabase/client";
export function DashboardShell({children,email}:{children:React.ReactNode;email:string}){const path=usePathname(),router=useRouter();const [open,setOpen]=useState(false);async function logout(){await createClient().auth.signOut();router.push("/");router.refresh()}const nav=[{href:"/dashboard",Icon:LayoutDashboard,label:"Overview"},{href:"/dashboard/new",Icon:Plus,label:"Build aplikasi"}];return <div className="min-h-screen bg-[#050505] lg:grid lg:grid-cols-[224px_1fr]"><header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-white/[.07] bg-black/75 px-5 backdrop-blur-xl lg:hidden"><Logo/><button onClick={()=>setOpen(!open)} aria-label="Menu" className="p-2">{open?<X/>:<Menu/>}</button></header><aside className={`${open?"block":"hidden"} fixed inset-x-0 top-16 z-30 border-b border-white/[.08] bg-[#080808] p-5 lg:sticky lg:top-0 lg:block lg:h-screen lg:border-b-0 lg:border-r`}><Logo/><div className="mt-9"><p className="px-3 text-[9px] font-black uppercase tracking-[.2em] text-zinc-700">Workspace</p><nav className="mt-3">{nav.map(({href,Icon,label})=><Link onClick={()=>setOpen(false)} key={href} href={href} className={`mb-1 flex items-center gap-3 rounded-md px-3 py-2.5 text-xs font-bold transition ${path===href?"bg-white text-black":"text-zinc-600 hover:bg-white/[.05] hover:text-white"}`}><Icon size={15}/>{label}</Link>)}</nav></div><div className="mt-5 border-t border-white/[.07] pt-5 lg:absolute lg:bottom-5 lg:left-5 lg:right-5"><p className="truncate px-3 text-[10px] text-zinc-700">{email}</p><button onClick={logout} className="mt-2 flex w-full items-center gap-3 rounded-md px-3 py-2.5 text-xs font-bold text-zinc-600 hover:bg-white/[.05] hover:text-white"><LogOut size={14}/>Keluar</button><p className="mt-5 px-3 text-[9px] text-zinc-800">Powered by XyStudio&apos;s</p></div></aside><main className="mesh min-w-0 p-5 sm:p-8 lg:p-10">{children}</main></div>}
