import type {BuildStatus} from "@/lib/types";
const styles:Record<BuildStatus,string>={queued:"border-zinc-700 text-zinc-500",building:"border-white/30 bg-white/[.06] text-white",success:"border-white bg-white text-black",failed:"border-red-900/60 bg-red-950/30 text-red-400"};const labels:Record<BuildStatus,string>={queued:"Antrean",building:"Building",success:"Selesai",failed:"Gagal"};export function StatusBadge({status}:{status:BuildStatus}){return <span className={`inline-flex w-fit rounded-sm border px-2 py-1 text-[9px] font-black uppercase tracking-wider ${styles[status]}`}>{labels[status]}</span>}
